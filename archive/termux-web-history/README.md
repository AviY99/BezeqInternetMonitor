# Historical Termux/Web artifacts

This directory is an archive inventory, not the active implementation.

Use the stable reference at:

- `../../termux/monitor_v5.py`
- `../../web/dashboard_v5_rooms.py`

See `../../docs/LEGACY_HISTORY.md` for the full lineage and status of every retained historical project file. `ARCHIVE_MANIFEST.json` records byte size and SHA-256 for every retained source/launcher artifact.
